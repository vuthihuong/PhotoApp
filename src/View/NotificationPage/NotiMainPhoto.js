import React, { Component } from 'react';
import {
    StyleSheet, Text, View, Image, TextInput, TouchableOpacity, ScrollView, ListView
} from 'react-native';

export default class NotiMainPhoto extends Component {
    render(){ 
        return(
            <View><Text>Hello World</Text></View>
           
        )
    }
}